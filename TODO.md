# Admin Dashboard Fixes - TODO

## ✅ Done

- [x] Fix 1: `isPaid` set to `true` when order status changes to "delivered"
- [x] Fix 2: Dashboard stats count ALL orders, not just paid ones
- [x] Fix 3: Order status dropdown doesn't snap back (uses statusDrafts)
- [x] Fix 4: Added admin "Mark as Paid" feature for COD orders (backend route + frontend button)
- [x] Fix 5: Fixed checkout `setProcessing(false)` missing when Razorpay script fails to load
